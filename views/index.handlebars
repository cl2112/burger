

<h1>Eat-Da-Burger!</h1>
<h4>Keeping Track of All the Burgers You've Eaten, An' Wan't To Eat.</h4>
<img src="assets/img/boiger.png">

<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
      <li>
        {{this.burger_name}}

        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="eaten" value="true">
          <button type="submit">EATEN!</button>
        </form>

      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      <li>
        {{this.burger_name}}

        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="eaten" value="false">
          <button type="submit">I'M GONNA EAT IT AGAIN!</button>
        </form>

      </li>
    {{/if}}
  {{/each}}
</ul>



<form action="/" method="POST">

  <div>
    <label for="burger_name">Enter the name of a burger:</label>
    <input type="text" id="burger_name" name="burger_name">
  </div>

  <button type="submit">Add Burger</button>
</form>


